function Subscription(price, dateBought, expirationDate, subscribedFor) {
    this.price = price;
    this.dateBought = dateBought;
    this.expirationDate = expirationDate;
    this.subscribedFor = subscribedFor;
}

export default Subscription;