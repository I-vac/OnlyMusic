function Track(artist, trackId, trackUri, dateAdded, favourite) {
    this.artist = artist;
    this.trackId = trackId;
    this.trackUri = trackUri;
    this.dateAdded = dateAdded;
    this.favourite = favourite;
}
export default Track;